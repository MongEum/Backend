spring:
  application:
    name: Backend

  profiles:
    active: local

  # JPA common configuration
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

# JWT configuration
jwt:
  secret: your-secret-key-here-must-be-at-least-256-bits-long-for-hs256-algorithm-please-change-in-production
  expiration: 86400000  # 24 hours in milliseconds
